# Contributing

1. Fork the repo
2. Create a feature branch (`git checkout -b feat/my-feature`)
3. Commit using conventional commits (`feat: add X`)
4. Push and open a PR

## Quality

This project uses [cpm](https://github.com/rkristelijn/cpm) for quality enforcement.
Run `cpm check` before pushing.
